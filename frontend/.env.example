# Backend API base URL.
# Local dev: leave as-is (matches the FastAPI default).
# Production: set to your deployed backend, e.g. https://qwen-tts-demo.onrender.com
VITE_API_BASE=http://127.0.0.1:8000
